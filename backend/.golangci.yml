version: "2"

run:
  timeout: 10m
  tests: false

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - unused
